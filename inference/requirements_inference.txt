# Dependencias para test_inference.py

# Core dependencies
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0

# LoRA support
peft>=0.7.0

# Quantization (opcional, solo si usas quantización)
bitsandbytes>=0.41.0

# RL agent (TD3)
stable-baselines3>=2.3.0
gymnasium>=0.29.0

# Metrics
textstat>=0.7.0

# HuggingFace Hub (y sus dependencias)
huggingface-hub>=0.19.0
filelock>=3.13.0
